# Database (PostgreSQL) - use docker-compose up to start
DATABASE_URL=postgresql://midl:midl_dev_password@localhost:5432/midl

# Redis (for resumable streams) - use docker-compose up to start


# AI Provider (set ONE of these - OpenAI is preferred if both are set)
ANTHROPIC_API_KEY=sk-ant-...

# MIDL Network Configuration

DATABASE_URL=postgresql://midl:midl_dev_password@localhost:5432/midl
REDIS_URL=redis://localhost:6379
OPENAI_API_KEY=sk-proj-...
MIDL_RPC_URL=https://rpc.staging.midl.xyz
MIDL_MEMPOOL_URL=https://mempool.staging.midl.xyz
MIDL_EXPLORER_URL=https://blockscout.staging.midl.xyz
MIDL_CHAIN_ID=14969

# Optional: Server-side signing (for operations without wallet)
MIDL_PRIVATE_KEY=0x...
